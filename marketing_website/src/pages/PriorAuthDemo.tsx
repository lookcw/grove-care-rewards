import { PriorAuthTable } from '@/components/prior-auth/PriorAuthTable';

const PriorAuthDemo = () => {
  return (
    <div className="min-h-screen bg-gray-50">
      <PriorAuthTable />
    </div>
  );
};

export default PriorAuthDemo;
