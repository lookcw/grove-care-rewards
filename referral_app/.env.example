# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=referral_db

# API Configuration
VITE_API_URL=http://localhost:8000/api

# Vite Proxy Target (for Docker Compose)
# Use 'http://backend:8000' when running in Docker Compose
# Use 'http://localhost:8000' when running locally without Docker
VITE_PROXY_TARGET=http://localhost:8000

# Gmail Service Account Configuration (OAuth2)
# Required for local development - add these to your local backend/.env file
# For production/staging, the service account JSON is stored in Google Cloud Secret Manager
# IMPORTANT: GMAIL_SENDER_EMAIL must be a real Google Workspace user account
GMAIL_SERVICE_ACCOUNT_FILE=/path/to/service-account-key.json
GMAIL_SENDER_EMAIL=help@grovehealth.us
